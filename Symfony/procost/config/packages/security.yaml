security:
    encoders:
        App\Entity\User:
            algorithm: auto

    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true
            lazy: true
            provider: app_user_provider
            guard:
                authenticators:
                    - App\Security\LoginFormAuthenticator
            logout:
                path: app_logout
                # where to redirect after logout
                # target: app_any_route

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
#        Accès autorisé non connectés
#        - { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY }
#        Accès autorisés pour les Managers et les Employes
#        - { path: ^/, roles: [ ROLE_MANAGER, ROLE_EMPLOYEE ] }
#        - { path: ^/project, roles: [ ROLE_MANAGER, ROLE_EMPLOYEE ] }
#        - { path: ^/employee, roles: [ ROLE_MANAGER, ROLE_EMPLOYEE ] }
#        - { path: ^/metier, roles: [ ROLE_MANAGER, ROLE_EMPLOYEE ] }
##        Accès autorisés pour les Managers
#        - { path: ^/project/modify, roles: ROLE_MANAGER }
#        - { path: ^/employee/modify, roles: ROLE_MANAGER }
#        - { path: ^/metier/modify, roles: ROLE_MANAGER }
#        - { path: ^/metier/delete, roles: ROLE_MANAGER }
